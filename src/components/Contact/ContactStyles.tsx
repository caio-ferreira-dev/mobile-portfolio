import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    gap: 15
  },
  text: {
    width: 'auto',
    padding: 5,
    color: 'white',
    fontSize: 24,
    textAlign: 'center',
    borderRadius: 15,
  }
});